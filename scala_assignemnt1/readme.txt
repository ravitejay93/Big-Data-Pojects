Q1) Move the file into the cluster and run spark-shell -i question_1.scala.
Q2) Move the file into the cluster and run spark-shell -i question_2.scala. When "name" promt occurs, INPUT the user name.
Q3) Move the file into the cluster and run spark-shell -i question_3.scala.
Q4) Move the file into the cluster and run spark-shell -i question_4.scala.
Q5) Move the file into the cluster and run spark-shell -i question_5.scala.
